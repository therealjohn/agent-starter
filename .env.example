# Required
ANTHROPIC_API_KEY=your_api_key_here

# Optional
# AGENT_MODEL=sonnet          # sonnet | opus | haiku
# AGENT_MAX_TURNS=100
# API_PORT=3000

# Session isolation strategy: local | docker | azure (default: local)
# SESSION_STRATEGY=local

# Local strategy settings
# SESSION_BASE_DIR=./sessions  # Base path for session directories
# SESSION_CLEANUP=false        # Clean up session dirs after query completes

# Docker strategy settings
# DOCKER_IMAGE=agent-starter-api  # Docker image for ephemeral containers

# Azure strategy settings (required when SESSION_STRATEGY=azure)
# AZURE_SESSION_POOL_ENDPOINT=   # Azure Dynamic Sessions pool management endpoint
# AZURE_CLIENT_ID=               # Managed identity client ID
# SESSION_POOL_AUDIENCE=https://dynamicsessions.io/.default
